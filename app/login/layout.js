export default function LoginLayout({ children }) {
    return (
        <>
        {children}
        </>
    )
}
export const metadata = {
  title: "Login",
  robots: {
    index: false,
    follow: false,
  },
};